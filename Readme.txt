To build the project type make. Cd into bin and run ./mysh. The commands can be run without ./ because we set the path to include the bin folder.


You can use cd and pwd within the shell because they are "aliased" to mycd and mypwd.
